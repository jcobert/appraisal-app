# pnpm configuration
auto-install-peers=true
strict-peer-dependencies=false

# Enable shamefully-hoist to fix ESLint module resolution issues
# This is necessary for ESLint plugins to find their dependencies in monorepo
shamefully-hoist=true
