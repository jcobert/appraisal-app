/**
 * Element ID provided to the layout that contains the app's main UI.
 * This layout excludes things like singleton containers (e.g. toast) and providers.
 */
export const APP_MAIN_UI_LAYOUT_ID = '_app-layout-main-ui'
